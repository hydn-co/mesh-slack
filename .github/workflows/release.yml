name: Release

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    name: Release
    permissions:
      contents: write
    uses: hydn-co/build-tools/.github/workflows/collector-release.yml@main
